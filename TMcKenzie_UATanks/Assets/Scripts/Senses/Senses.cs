using UnityEngine;

public class Senses : MonoBehaviour
{
    [SerializeField] bool visionEnabled;
    [SerializeField] bool hearingEnabled;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }


}
